# Actions that need to be possible through the api:
# - Initialize app if required ?
#  - Is it ok to require cli setup for first time use? I think so, as esi-auth setup is definatley required.
# - update schema store ? this can be cli only
# - make EsiQueries, get results
#  - build queries,

#
